package config

var (
	K = 0 // Origin shard nums

	LL_Shard = 0             // Indicate LL belongs to which shard
	RandSeed = int64(114614) // Use in RFC Encoding

	Verify_Interval = 10 // request verify interval
)
